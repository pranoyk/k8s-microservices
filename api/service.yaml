apiVersion: v1
kind: Service
metadata:
  name: k8s-demo-api-service
spec:
  selector:
    app.kubernetes.io/name: k8s-demo-api
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30017
